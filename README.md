# Progressive-Resize-Illustrator-JSX
ScriptUI tool to apply progressive size changes to selected objects. Supports height/width or uniform scaling, Increment (px/% step) and Range (start→end with easing). Includes ordering (left→right, top→bottom, layer order + reverse), 3\times3 anchor, and optional stroke/effects scaling.
